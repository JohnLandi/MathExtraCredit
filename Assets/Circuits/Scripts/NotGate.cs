using UnityEngine;
using System.Collections;

public class NotGate : MonoBehaviour {


	
	// Update is called once per frame
	void Update () 
	{
		//if(GetComponentInParent<Switch>().on = true)
			

	}
}
